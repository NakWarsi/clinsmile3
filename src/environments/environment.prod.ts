export const environment = {
  production: true,
  // Set this to false to disable ALL editing functionality across the entire application
  ENABLE_EDITING: false  // Production is always non-editable
};
